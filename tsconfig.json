{
  "compilerOptions": {
    "target": "ES2020",                        
    "module": "CommonJS",                      // For Jest compatibility
    "lib": ["DOM", "DOM.Iterable", "ESNext"], // Browser APIs + latest JS
    "jsx": "react-jsx",                        // React 17+ JSX transform
    "esModuleInterop": true,                   
    "moduleResolution": "node",                
    "allowJs": true,                          
    "isolatedModules": false,                   
    "noEmit": true,                            // Don't output compiled files
    "strict": true                             // Enable strict type checking
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}



// This configuration file sets up TypeScript for a React project, enabling strict type checking and module resolution.
// It allows JavaScript files, skips type checking for library files, and supports modern ECMAScript features.
// The `jsx` option is set to `react-jsx`, which is suitable for


// React 17 and later versions.
// The `include` array specifies that all files in the `src` directory should be included
// The `include` array specifies that all files in the `src` directory should be included
